const o = {
    getValue(inputElement) {
        return inputElement.value;
    },
    setValue(inputElement, value) {
        inputElement.value = value;
    }
}; // javascript got crazy since i was using it
return o; // o